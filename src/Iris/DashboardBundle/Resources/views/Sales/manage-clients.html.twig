{# empty Twig template #}
{# empty Twig template #}
{% extends 'DashboardBundle:Sales:manage-application.html.twig'%}
{% block head %}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"> 
$(document).ready(function(){
    $('.del').easyconfirm(); 
 $(".del").click(function(){
    if (!confirm("Do you want to delete")){
      return false;
    }
  });
    
});
</script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>

<style type="text/css">
    h1{
        margin: 30px 0;
        padding: 0 200px 15px 0;
        border-bottom: 1px solid #E5E5E5;
    }
	.bs-example{
    	margin: 20px;
    }
    .form-style-2-heading{
    font-weight: bold;
    font-style: italic;
    border-bottom: 2px solid #ddd;
    margin-bottom: 40px;
    margin-top:40px;
    font-size: 15px;
    padding-bottom: 3px;
}

.material-icons {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: middle;
}
    .allClient h4{
        font-weight: bold;
        font-style: italic;
        border-bottom: 2px solid #ddd;
        margin-bottom: 30px;
        margin-top: 20px;
        font-size: 20px;
        padding-bottom: 3px;
    }
    td{
        font-family: serif;
        color: #616161 !important;
        font-style: italic;

    }
    small a{
       margin-top: 5px !important;
    }
    .font-b{
        font-family: serif;
        color: #616161;
        font-style: italic;
        margin-left: 15px;
    }
</style>
 
 {% endblock  %}
{% block breadcrumb %}

Sales  &rsaquo; Manage Clients

{% endblock %}

{% block body %}

<div style="width:80%; margin-left:10%; margin-top:5%; margin-bottom:10%;">
    <div class="del allClient"><h4>All Clients</h4></div>
<div class="datagrid">
 <table>

        <thead>
          <tr>
              
              <th>Name</th>
              <th>Bundles</th>
              <th>Applications</th>
              <!-- <th>Operations</th> -->
              
          </tr>
        </thead>

        <tbody>
        {% for client in clients %}
          <tr>
                          
            <td>&nbsp;&nbsp;
                <b class="font-b">{{ client.Name }}</b>
                <br style="line-height:180%;">

                <a onclick="return confirm('Are you sure you want to delete  {{client.Name}} ');" href="{{ path('dashboard_sales_delete_client', {'slug': client.ID}) }}" style="margin-left: 4%"> <i class="material-icons" style="font-size:15px">delete</i>Delete</a>
                <a  href="{{ path('dashboard_sales_client_profile', {'slug': client.ID}) }}"style="margin-left: 4%"><i class="material-icons" style="font-size:15px">mode_edit</i>View/Edit</a> 
            </td>
            <td><a href="{{ path('dashboard_sales_clients_bundles', {'slug': client.ID}) }}">{{ client.numberOfBundles }}</a> </td>
          <td><a href="{{ path('dashboard_sales_clients_applications', {'slug': client.ID}) }}">{{ client.numberOfApplications }}</a> </td>
      
          </tr>
          {% endfor %}
        </tbody>
      </table>
          
      </div>
        <br>
  
      
</div>
  
{% endblock %}