<div class="fd-navigation mdl-layout__drawer">
     <span class="centerStore" style="  font-size: 20px;
        font-family: sans-serif; height: 49px;background-color: #0083ba;padding: 7px 29px; ">
         <a href="{{ path('homepage') }}" style="color: white;margin-left: 10px;    ">
             {#<img src="{{ asset('images/LayerGray1.png') }}" width="30px;"#}
                                          {##}
                                                 {#height="70%"> #} <span style="    font-size: 25px;
    font-style: italic;
    font-family: serif;
    color:white; margin-top: 10px;"> Sensory Store</span></a>
        </span>
    <span class="mdl-layout-title  mdl-color-text--white" style="font-family: Serif; background: gainsboro;">
        <a href="{{ path('homepage') }}" style="color: #555555!important;"><div style=" display: inline;
  " ></div> &nbsp; Apps </a></span>
    <span class="mdl-layout-title mdl-color-text--white gray" style="background-color:#555555">
        <a href="#"> Categories </a>
    </span>
    <nav class="mdl-navigation mdl-color--white">

        {% for category in categories %}
            <a class="{% if category.slug == slug_selected %}mdl-color--accent mdl-color-text--white{% endif %} mdl-navigation__link" href="{{ path('category', {'slug': category.slug}) }}">
                <i class="material-icons">{{ category.icon }}</i> {{ category.name }}
            </a>
        {% endfor %}
    </nav>
    <span class="mdl-layout-title mdl-color-text--white" style="background-color:#555555">
        <a href="#"> Bundles </a>
    </span>
    <nav class="mdl-navigation mdl-color--white gray">
        {% for bundle in bundles %}
            <a class="mdl-navigation__link" href="{{ path('bundle', {'slug': bundle.ID}) }}">
                <i class="material-icons">grade</i> {{ bundle.Name }}
            </a>
        {% endfor %}
    </nav>
</div>